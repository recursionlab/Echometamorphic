# Echometamorphic

Tools for exploring reflective code transformations and complexity analysis in Python.

ψₙ₊₁ := ΨAgent(ψₙ) = Reflect(Drift(Collapse(ψₙ)))

## Example

Run the recursive complexity checker on a module:

```bash
recursive_complexity_check path/to/file.py
```
