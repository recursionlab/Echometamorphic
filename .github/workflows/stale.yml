name: Close Stale Issues and PRs

on:
  schedule:
    - cron: '0 0 * * *'

jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/stale@v9
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          stale-issue-message: 'This issue is stale because it has been open 30 days with no activity.'
          stale-pr-message: 'This pull request is stale because it has been open 30 days with no activity.'
          close-issue-message: 'Closing this issue due to prolonged inactivity.'
          close-pr-message: 'Closing this PR due to prolonged inactivity.'
          stale-issue-label: stale
          stale-pr-label: stale
          exempt-issue-labels: pinned,security,work-in-progress,discussion,awaiting-feedback,do-not-close
          exempt-pr-labels: pinned,security,work-in-progress,discussion,awaiting-feedback,do-not-close
          remove-stale-when-updated: true
          days-before-stale: 30
          days-before-close: 7
